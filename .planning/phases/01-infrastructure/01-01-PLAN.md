---
phase: 01-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - tsconfig.json
  - .nvmrc
  - .gitignore
  - src/
  - public/
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "Running `npm run dev` starts a local Astro dev server on localhost"
    - "Running `npm run build` produces a static site in the dist/ directory without errors"
    - "The project uses the AstroWind template as its foundation"
    - "The project is committed to a GitHub repository on the main branch"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with Astro and AstroWind dependencies"
      contains: "astro"
    - path: "astro.config.mjs"
      provides: "Astro configuration with static output mode"
      contains: "output"
    - path: ".nvmrc"
      provides: "Node.js version pinning"
      contains: "20"
    - path: ".gitignore"
      provides: "Git ignore rules for Node/Astro project"
      contains: "node_modules"
  key_links:
    - from: "package.json"
      to: "astro.config.mjs"
      via: "astro dependency drives config"
      pattern: "astro"
---

<objective>
Scaffold the yoga teacher website from the AstroWind template and push to GitHub.

Purpose: Establish the foundational Astro project that all subsequent phases will build upon. This is a local-only phase — no Netlify deployment yet.

Output: A working AstroWind-based Astro project committed to GitHub, buildable locally with `npm run dev` and `npm run build`.

NOTE on requirement coverage: INFRA-01 through INFRA-05 are assigned to Phase 1 in the ROADMAP, but per user decision in CONTEXT.md, only the local scaffold + GitHub push portion is delivered here. INFRA-01 (deploy to Netlify from GitHub) is partially addressed by creating the GitHub repo. INFRA-02 through INFRA-05 (auto-deploy, HTTPS/domain, Netlify Identity, Git Gateway) will be fully delivered in Phase 5.
</objective>

<execution_context>
@D:/Work/yoga/yoga-web/.claude/get-shit-done/workflows/execute-plan.md
@D:/Work/yoga/yoga-web/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold AstroWind project and verify local build</name>
  <files>package.json, astro.config.mjs, tsconfig.json, .nvmrc, .gitignore, src/, public/</files>
  <read_first>
    - .planning/phases/01-infrastructure/01-CONTEXT.md
    - .planning/research/STACK.md
    - .planning/research/ARCHITECTURE.md
  </read_first>
  <action>
    1. Clone the AstroWind template into the current project directory. Use the following approach:
       - Run: `npx degit onwidget/astrowind .` to clone the AstroWind template into the current directory (D:\Work\yoga\yoga-web). If `degit` fails because the directory is not empty (due to .planning/ and CLAUDE.md), use an alternative: clone into a temp directory and move files.
       - Alternative approach if degit fails: `git clone --depth 1 https://github.com/onwidget/astrowind.git /tmp/astrowind-template` then copy all files (except .git) from /tmp/astrowind-template into D:\Work\yoga\yoga-web, being careful not to overwrite .planning/ or CLAUDE.md.

    2. Install dependencies: `npm install`

    3. Create `.nvmrc` file at project root with content: `20` (pins Node.js 20 LTS)

    4. Verify `astro.config.mjs` exists and contains `output: 'static'` (or add it if missing). The AstroWind template should already have this, but confirm explicitly. If the config uses `defineConfig`, ensure the `output` field is set to `'static'`.

    5. Verify `.gitignore` exists and includes at minimum: `node_modules`, `dist`, `.astro`. AstroWind should provide this. If not, create one with these entries.

    6. Run `npm run build` to verify the project builds without errors. The build should produce output in the `dist/` directory.

    7. Run `npm run dev` briefly to verify the dev server starts (kill after confirming it starts successfully on a localhost port).

    NOTE: Do NOT modify the AstroWind template content yet. Phase 2 will handle branding and content customization. The goal here is a clean, working scaffold.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists and contains `"astro"` as a dependency (grep: `"astro"`)
    - `astro.config.mjs` exists at project root
    - `.nvmrc` exists and contains the string `20`
    - `.gitignore` exists and contains `node_modules`
    - `npm run build` exits with code 0
    - `dist/` directory exists after build with at least one `.html` file
  </acceptance_criteria>
  <done>AstroWind template is scaffolded, dependencies installed, project builds successfully, .nvmrc pins Node 20</done>
</task>

<task type="auto">
  <name>Task 2: Initialize git repository and push to GitHub</name>
  <files>.git/, .gitignore</files>
  <read_first>
    - .gitignore
    - .planning/phases/01-infrastructure/01-CONTEXT.md
  </read_first>
  <action>
    1. The repository already has a git init (master branch exists per git status). Switch to main branch:
       - Run: `git checkout -b main` (or `git branch -M main` if already on master with commits)

    2. Stage all project files for commit. Be explicit about what to add:
       - `git add .` (the .gitignore should already exclude node_modules, dist, .astro)
       - Verify no unwanted files are staged: `git status` — confirm node_modules and dist are NOT listed

    3. Create the initial commit:
       - Message: "feat(01-infrastructure): scaffold AstroWind Astro project"

    4. Create a GitHub repository using the GitHub CLI:
       - Run: `gh repo create yoga-web --public --source=. --push`
       - This creates the repo on GitHub, sets the remote, and pushes the main branch in one command.
       - If `gh` is not available or the repo already exists, fall back to:
         a. `git remote add origin https://github.com/USER/yoga-web.git` (replace USER with the actual GitHub username)
         b. `git push -u origin main`

    5. Verify the push succeeded:
       - Run: `git log --oneline -1` to confirm the commit exists
       - Run: `git remote -v` to confirm the remote is set

    NOTE: The GitHub repo name should be `yoga-web`. If the user's GitHub account already has a repo with this name, the push step will handle it.
  </action>
  <verify>
    <automated>git log --oneline -1 && git remote -v</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` outputs `main`
    - `git log --oneline -1` shows a commit with message containing "scaffold AstroWind"
    - `git remote -v` shows an origin remote pointing to a GitHub URL containing "yoga-web"
    - `git status` shows a clean working tree (no untracked project files — .planning files may be untracked and that is acceptable)
  </acceptance_criteria>
  <done>Project is committed on main branch and pushed to a GitHub repository named yoga-web</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `git log --oneline -1` shows the initial commit
3. `git remote -v` shows GitHub origin
4. `.nvmrc` contains `20`
5. The project directory contains standard AstroWind files (src/, public/, astro.config.mjs, package.json)
</verification>

<success_criteria>
- AstroWind-based Astro project builds locally without errors
- Project is on the `main` branch with at least one commit
- Project is pushed to a GitHub repository
- Node.js version is pinned to 20 via .nvmrc
- No Netlify configuration exists (deferred to Phase 5)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-01-SUMMARY.md`
</output>
